class Persona {
    constructor(firstName, lastdName){
        this.firstName = firstName;
        this.lastdName = lastdName
    }

    fullName (){
        console.log(newPerson.firstName + " " + newPerson.lastdName);
    }
}

const newPerson = new Persona("Pablo", "Gómez");
// newPerson.fullName();
